import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },

  boxTitle: {
    marginTop: 15,
    marginBottom: 5,
  },

  title: {
    fontSize: 16,
    fontWeight: 'bold',
    color: '#666',
  },

  media: {
    width: 150,
    height: 150,
    borderRadius: 7,
    overflow: 'hidden',
  },

  image: {
    width: '100%',
    height: '100%',
  },
});

export default styles;
