import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {},
  boxInput: {
    marginBottom: 20,
  },
  boxButton: {
    marginTop: 30,
  },
});

export default styles;
