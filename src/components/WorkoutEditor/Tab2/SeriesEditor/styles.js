import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    marginTop: 20,
  },

  content: {
    flexDirection: 'row',
  },

  containerSeries: {
    flex: 1,
    marginHorizontal: 10,
  },

  containerRepetition: {
    flex: 1,
  },
});

export default styles;
