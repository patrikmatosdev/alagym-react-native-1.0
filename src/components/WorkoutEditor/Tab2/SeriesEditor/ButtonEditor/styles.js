import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: 'row',
    marginTop: 10,
  },

  containerButton: {
    padding: 10,
    flex: 1,
  },
});

export default styles;
