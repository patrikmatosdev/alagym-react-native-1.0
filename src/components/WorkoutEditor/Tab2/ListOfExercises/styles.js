import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    borderWidth: 0.6,
    borderTopColor: '#ccc',
    backgroundColor: '#ddd',
    padding: 15,
  },

  description: {
    padding: 20,
  },

  image: {
    width: 80,
    height: 60,
    borderRadius: 10,
  },

  title: {
    color: '#CCC',
    fontSize: 22,
  },
});

export default styles;
